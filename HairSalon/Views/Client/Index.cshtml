@{
  Layout = "_Layout";
}
@using HairSalon.Models;

<h2>🌺Clients🌺</h2>

@if (@Model.Count == 0)
{
  <h3>You have no clients!</h3>
} 
else
{
  <ul>
    @foreach (Client client in Model)
    {
      <li>@Html.ActionLink($"{client.Name}", "Details", new { id = client.ClientId }) | @client.StylistId</li>
    }
  </ul>
}

<p>@Html.ActionLink("Add client", "Create")</p>
<p>@Html.ActionLink("Back to Homepage", "Index", "Home")</p>

