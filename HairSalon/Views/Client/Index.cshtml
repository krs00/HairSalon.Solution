@{
  Layout = "_Layout";
}
@using HairSalon.Models;

<h2>🌺Clients Page🌺</h2>

@if (Model.Count == 0)
{
  <br>
  <br>
  <h3>No clients have been added!</h3>
  <br> 
  <br>
}

<ul>
  @foreach (Client client in Model)
  {
   <h4> <li>@client.ClientId | @Html.ActionLink($"{client.Name}", "Details", new { id = client.ClientId })</li> </h4>
  }
</ul>

<br>
<br>
<h3>@Html.ActionLink("Back to Homepage", "Index", "Home")</h3>

