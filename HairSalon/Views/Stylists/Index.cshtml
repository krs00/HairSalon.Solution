@{
  Layout = "_Layout";
}

@using HairSalon.Models;

<h2>🌺Stylist Page🌺</h2> 
<br>
<br>
@if (Model.Count == 0)
{
  <h3>No stylists have been added!</h3>
  <br>
}

<ul>
  @foreach (Stylist stylist in Model)
  {
   <h4> <li>@stylist.StylistId | @Html.ActionLink($"{stylist.Name}", "Details", new { id = stylist.StylistId })</li> </h4>
  }
</ul>

<br>
<br>

<h3>@Html.ActionLink("Add stylist", "Create")</h3>
<br>
<h3>@Html.ActionLink("Back to Homepage", "Index", "Home")</h3>
